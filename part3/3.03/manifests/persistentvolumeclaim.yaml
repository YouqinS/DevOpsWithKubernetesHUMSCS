#apiVersion: v1
#kind: PersistentVolumeClaim
#metadata:
#  name: pv-claim
#spec:
#  storageClassName: standard
#  accessModes:
#    - ReadWriteOnce
#  resources:
#    requests:
 #     storage: 1Gi

#https://cloud.google.com/kubernetes-engine/docs/concepts/persistent-volumes#dynamic_provisioning
#  create a PersistentVolumeClaim and Kubernetes automatically provisions a persistent disk
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi